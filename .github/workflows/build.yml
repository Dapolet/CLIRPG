name: Build Cross-Platform

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: rpg-linux
            binary_name: rpgcli-linux
            binary_path: ./build/rpgcli-linux
          - os: macos-latest
            artifact_name: rpg-macos
            binary_name: rpgcli-macos
            binary_path: ./build/rpgcli-macos
          - os: windows-latest
            artifact_name: rpg-windows
            binary_name: rpgcli-win
            binary_path: ./build/rpgcli-win.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- macOS: clang++ уже установлен ---
      - name: Build (macOS)
        if: runner.os == 'macOS'
        run: make BIN_NAME=${{ matrix.binary_name }}

      # --- Linux: g++ и make уже установлены на ubuntu-latest ---
      - name: Build (Linux)
        if: runner.os == 'Linux'
        run: make BIN_NAME=${{ matrix.binary_name }}

      # --- Windows: ставим MSYS2 с GCC и make ---
      - name: Setup MSYS2 (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64          # ← Ключевое: явно выбираем среду
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            make

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}          # ← Key fix: must include {0}
        run: make BIN_NAME=${{ matrix.binary_name }}

      # --- Проверяем, что бинарник действительно создан ---
      - name: Verify binary (Linux)
        if: runner.os == 'Linux'
        run: ls -la ./build/rpgcli-linux && file ./build/rpgcli-linux

      - name: Verify binary (macOS)
        if: runner.os == 'macOS'
        run: ls -la ./build/rpgcli-macos && file ./build/rpgcli-macos

      - name: Verify binary (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}          # ← Also needs fixing here
        run: ls -la ./build/rpgcli-win.exe && file ./build/rpgcli-win.exe

      # --- Загружаем артефакт ---
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.binary_path }}
          retention-days: 30

  # --- Создаём GitHub Release с бинарниками при пуше тега v* ---
  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true